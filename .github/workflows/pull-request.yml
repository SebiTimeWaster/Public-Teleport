name: Pull Request
run-name: Validate Pull Request
on: [pull_request]
jobs:
    Explore-GitHub-Actions:
        runs-on: ubuntu-latest
        steps:
            - name: Duplicate Code Cross-Check
              runs:
                  using: 'node24'
                  main: 'main.js'
              uses: astubbs/duplicate-code-cross-check@v1.0.0
