name: Pull Request
run-name: Valide Pull Request
on: [pull_request]
runs:
    using: 'node24'
    main: 'main.js'
jobs:
    Explore-GitHub-Actions:
        runs-on: ubuntu-latest
        steps:
            - name: Duplicate Code Cross-Check
              uses: astubbs/duplicate-code-cross-check@v1.0.0
